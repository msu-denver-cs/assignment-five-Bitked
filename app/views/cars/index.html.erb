<p id="notice"><%= notice %></p>

<h1>Cars</h1>

<%= form_tag(search_cars_url, method: "get") do %>
    <%= label_tag(:query, "Search by Vin:") %>
    <%= text_field_tag :search, params[:search], data: { autocomplete: autocomplete_car_vin_cars_path } %><%= submit_tag("Search") %> 
    <%= submit_tag("Search") %>
<% end %>

<br/><div id="results"></div>

<script>
var QueryURL = "http://localhost:3000/books/search.json?query=";
var ResultsDiv = "results";
var Field = "name";
var QueryTag = "query";
</script>

<script type="text/javascript" src="/javascripts/AJAXSearch.js"></script>


<table>
  <thead>
    <tr>
      <th>Vin</th>
      <th>Model</th>
      <th>Make</th>
      <th>Country</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.vin %></td>
        <td><%= car.model %></td>
        <td><%= car.make.make %></td>
        <td><%= car.make.country %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>

<%= link_to 'New Car', new_car_path %>
